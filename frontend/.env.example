# Default API is http://127.0.0.1:8000 (no proxy). Override if needed:
# REACT_APP_API_URL=https://your-api.example.com

# Advanced: empty string = same-origin requests; src/setupProxy.js forwards /specs, /api, … to FastAPI.
# REACT_APP_API_URL=
# REACT_APP_PROXY_TARGET=http://127.0.0.1:8000
